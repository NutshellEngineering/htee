package message

// MessageSeparator is printed between a rendered request and its response
// (or between successive intermediate messages) when interactive, mirroring
// httpie's MESSAGE_SEPARATOR.
const MessageSeparator = "\n"
